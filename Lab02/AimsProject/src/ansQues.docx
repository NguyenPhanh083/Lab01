Ex 8:
Cannot creat a constructor method to build DVD by category after creating 
a constructor method to build a DVD by title because it has the same 
parameter type ( String in detail). It cannot distinguish between category
and title as the parameters are all the same in Java.